export default function Utente(id, username, nome, cognome, email, password, ruolo) {
    this.id = id;
    this.nome = nome;
    this.cognome = cognome;
    this.email = email;
    this.username = username;
    this.password = password;
    this.ruolo = ruolo;
};